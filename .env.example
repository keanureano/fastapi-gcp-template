# Default values (overridden if matching GitHub Secrets exist).
# To override, add a secret in GitHub: Settings > Secrets > Actions > New repository secret.
MESSAGE="Default message (replaced if a GitHub Secret named MESSAGE exists)"
